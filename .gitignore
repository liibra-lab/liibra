node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Generated by `wrangler types` (npm run check / build); its content depends
# on whether the Worker bundle exists, so a committed snapshot can never
# satisfy a --check in every tree state.
worker-configuration.d.ts

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
